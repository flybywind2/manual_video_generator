[project]
name = "internal-system-manual-video-agent"
version = "0.1.0"
requires-python = ">=3.10,<3.11"
dependencies = [
  "fastapi",
  "pillow",
  "playwright",
  "pydantic",
  "uvicorn[standard]",
]

[project.optional-dependencies]
dev = [
  "httpx",
  "pytest",
]
