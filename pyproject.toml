[project]
name = "internal-system-manual-video-agent"
version = "0.1.0"
requires-python = "==3.13.14"
dependencies = [
  "fastapi",
  "pillow",
  "playwright",
  "pydantic",
  "supertonic==1.3.1",
  "uvicorn[standard]",
]

[project.optional-dependencies]
dev = [
  "httpx",
  "pytest",
]
